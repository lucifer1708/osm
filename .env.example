# ─── Object Storage ───────────────────────────────────────
# Leave ENDPOINT empty for standard AWS S3
ENDPOINT=
ACCESS_KEY=your-access-key
SECRET_KEY=your-secret-key
REGION=us-east-1

# ─── Auth ─────────────────────────────────────────────────
# Run `make gen-password` to generate a bcrypt hash
AUTH_USERNAME=admin
AUTH_PASSWORD_HASH=
# Leave AUTH_TOTP_SECRET empty; it is generated on first login
AUTH_TOTP_SECRET=

# ─── Provider examples ────────────────────────────────────
# AWS S3          ENDPOINT=  REGION=us-east-1
# MinIO (local)   ENDPOINT=http://localhost:9000  REGION=us-east-1
# Cloudflare R2   ENDPOINT=https://<account>.r2.cloudflarestorage.com  REGION=auto
# Backblaze B2    ENDPOINT=https://s3.<region>.backblazeb2.com  REGION=us-west-002
# Wasabi          ENDPOINT=https://s3.wasabisys.com  REGION=us-east-1
# Hetzner         ENDPOINT=<location>.your-objectstorage.com  REGION=<location>
